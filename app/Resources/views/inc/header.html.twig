{% block header %}
    <nav class="navbar navbar-default navbar-static-top">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="{{ url('homepage') }}">Advertisement</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li><a href="{{ url('homepage') }}">All Advertisements</a></li>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <li><a href="{{ path('user_user_listadvertisements') }}">Your Advertisements</a></li>
                    {% endif %}
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <li><a href="{{ path('advertisement_advertisement_create') }}">
                                <button class="btn btn-sm">Create Advertisement</button>
                            </a></li>
                      <li><p class="navbar-text">
                              {{ app.user.username }} |
                              <a class="navbar-link" href="{{ path('fos_user_security_logout') }}">Logout</a>
                          </p>
                      </li>
                    {% else %}
                         <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                        <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                    {% endif %}
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>
{% endblock %}

